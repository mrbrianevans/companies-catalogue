# Environment configuration for the saveFiles Go service
# Copy this file as needed and fill in the values. Do not commit real secrets.
# You can use this with docker-compose via `env_file:` or `--env-file` in `docker run`.

# Path to the generated metadata summary JSON used to decide what to download
# Default in code: /output/sftp_file_metadata_summary.json
METADATA_PATH=/output/sftp_file_metadata_summary.json

# Local directory where files will be downloaded (path structure preserved)
# Default in code: /output
OUTPUT_DIR=/output

# SFTP connection details
# Default host in code: bulk-live.companieshouse.gov.uk
SFTP_HOST=bulk-live.companieshouse.gov.uk
# Default port in code: 22
SFTP_PORT=22
# Required: your SFTP username for Companies House Bulk Data
SFTP_USERNAME=your_sftp_username
# Path to your private key inside the container/host
# Default in code: /root/.ssh/ch_key
SFTP_KEY_PATH=/root/.ssh/ch_key

# S3-compatible storage (e.g., Cloudflare R2) configuration
# For Cloudflare R2, endpoint looks like: https://<accountid>.r2.cloudflarestorage.com
S3_ENDPOINT=https://<accountid>.r2.cloudflarestorage.com
# Region; for R2 typically "auto"
S3_REGION=auto
# Required: access key ID for the bucket
S3_ACCESS_KEY_ID=your_access_key_id
# Required: secret access key for the bucket
S3_SECRET_ACCESS_KEY=your_secret_access_key
# Required: bucket name to upload files into
S3_BUCKET=your_bucket_name
